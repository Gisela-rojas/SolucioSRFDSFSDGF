import math


class EcuacionSegundoGrado:

    def calculoESO(self,a,b,c):
        discriminante = math.pow(b,2) -4 * a * c
        if discriminante >= 0:
            raizDiscriminante = math

        return None, None
